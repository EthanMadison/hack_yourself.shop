# hack_yourself.shop — учебный интернет‑магазин (модульная версия)

Этот проект — учебный интернет‑магазин на **Flask** с модульной структурой (Blueprint'ы и app factory).  
Данный проект представляет собой интернет магазин с:

- каталогом товаров и карточками,
- корзиной с автообновлением суммы,
- оформлением заказов,
- интеграцией оплаты через **Stripe Checkout** (в тестовом режиме),
- регистрацией, авторизацией и восстановлением пароля,
- личным кабинетом и историей заказов,
- профилем пользователя с аватаром по умолчанию и загрузкой своего,
- админ‑панелью для управления товарами, категориями и заказами,
- CLI‑командами для создания демо-данных и администратора.

---

## 📁 Структура проекта

```text
.
├─ app/
│  ├─ __init__.py            # create_app(), регистрация блюпринтов и расширений
│  ├─ extensions.py          # db, login_manager, конфигурация Stripe
│  ├─ models.py              # SQLAlchemy‑модели: User, Category, Product, Order, OrderItem
│  ├─ utils.py               # утилиты: корзина, валидация пароля, аватар по умолчанию, подсчёт суммы заказа
│  ├─ cli.py                 # CLI‑команды: seed, create-admin
│  └─ blueprints/
│     ├─ shop/
│     │  └─ routes.py        # публичные маршруты: каталог, корзина, оформление, Stripe
│     ├─ auth/
│     │  └─ routes.py        # регистрация/вход/выход, профиль, кабинет, восстановление пароля
│     └─ admin/
│        └─ routes.py        # админ‑панель: категории, товары, заказы
│
├─ templates/                # Jinja2‑шаблоны
│  ├─ base.html              # базовый макет
│  ├─ index.html             # каталог
│  ├─ product.html           # карточка товара
│  ├─ cart.html              # корзина с автообновлением суммы
│  ├─ checkout.html          # оформление заказа
│  ├─ thankyou.html          # страница «Спасибо за заказ»
│  ├─ login.html             # вход
│  ├─ register.html          # регистрация
│  ├─ password_forgot.html   # запрос сброса пароля
│  ├─ password_reset.html    # ввод нового пароля
│  ├─ account.html           # личный кабинет (список заказов)
│  ├─ account_order.html     # детали конкретного заказа
│  └─ admin/                 # шаблоны админ‑панели
│
├─ static/
│  ├─ style.css              # простой, аккуратный UI
│  ├─ img/                   # изображений товаров + стандартный аватар
│  └─ uploads/avatars/       # сюда попадают загруженные аватары пользователей
│
├─ run.py                    # точка входа (python run.py)
├─ requirements.txt          # зависимости
└─ README.md                 # этот файл
```

---

## ⚙️ Установка и запуск

### 1. Клонировать / распаковать проект

```bash
  cd hack_yourself.shop
```

### 2. Создать виртуальное окружение и установить зависимости

```bash
  python -m venv .venv
  
  # Windows:
  .venv\Scripts\activate
  
  # Linux / macOS:
  source .venv/bin/activate

  pip install -r requirements.txt
```

### 3. Запуск приложения

```bash
  python run.py
```

По умолчанию сервер поднимется на:

- http://127.0.0.1:5000/

---

## 💾 База данных и демоданные

Проект использует SQLite (файл `shop.db` в корне).

При первом запуске `create_app()` создаёт все таблицы.  
Чтобы наполнить базу демоданными (категории и товары), используйте CLI‑команду:

```bash
  flask --app run seed
```

Создаются категории: «Одежда», «Сувениры», «Стикеры» и несколько демо‑товаров.

---

## 👤 Администратор

Для управления товарами, категориями и заказами нужна учётка с флагом `is_admin=True`.

Администратора удобно создать через CLI:

```bash
  # Вариант с переменными окружения
export ADMIN_EMAIL=admin@mail.ru
export ADMIN_PASSWORD=Admin#123
flask --app run create-admin

# Или сразу аргументами
flask --app run create-admin --email admin@mail.ru --password Admin#123
```

После этого можно войти на сайт под этим email/паролем и открыть:

- `/admin` — главная страница админ‑панели.

---

## 🧱 Модели данных (models.py)

### User
Поля:
- `email` — уникальный email.
- `password_hash` — хэш пароля.
- `is_admin` — флаг администратора.
- `full_name` — имя/ФИО.
- `default_address` — адрес по умолчанию.
- `avatar` — путь к аватару (если пусто — используется `img/default_avatar.png`).
- `email_confirmed` — подтверждён ли email.
- `orders` — связь `User -> Order`.

Основные методы:
- `set_password(password)` — хэширует и сохраняет пароль.
- `check_password(password)` — проверяет пароль.

### Category / Product
- Category: простая модель с названием и связью `products`.
- Product: название, цена, описание, изображение и ссылка на категорию.

### Order / OrderItem
- Order:
  - связь на пользователя (`user_id`) — может быть `None` для гостя,
  - `customer_name`, `email`, `address`,
  - `created_at`, `status` (`new`, `pending`, `awaiting_payment`, `paid`, `canceled`),
  - `user_order_no` — порядковый номер заказа внутри аккаунта / email,
  - `stripe_session_id` — ID сессии Stripe (если использовалась оплата онлайн),
  - `items` — позиции заказа.
- OrderItem:
  - `product_id` - id товара,
  - `quantity` - количество товара,
  - `price_snapshot` — цена товара на момент покупки.

---

## 🔧 Вспомогательный код (utils.py, extensions.py, cli.py)

### utils.py

- `get_cart()` / `save_cart(cart)` — чтение и сохранение корзины в сессии пользователя.
- `cart_items(Product)` — преобразует содержимое корзины в список объектов/строк:
  - возвращает `[(product, qty, line_total), ...]` + общую сумму.
- `admin_required()` — проверка, что пользователь авторизован и `is_admin=True` (иначе `403`).
- `allowed_file(filename)` — проверка расширения для загружаемых картинок.
- `password_is_strong(password)` — проверяет политику сложности пароля:
  - минимум 6 символов,
  - хотя бы 1 заглавная буква,
  - хотя бы 1 цифра,
  - хотя бы 1 спецсимвол.
- `avatar_url_for(user)` — возвращает путь к аватару (либо пользовательский, либо `img/default_avatar.png`).
- `order_total(order)` — аккуратно считает сумму заказа по позициям.


### extensions.py

- `db` — экземпляр `SQLAlchemy`.
- `login_manager` — `Flask-Login` менеджер.
- Настройки Stripe:
  - `STRIPE_SECRET_KEY`,
  - `STRIPE_PUBLISHABLE_KEY`,
  - `STRIPE_CURRENCY` (по умолчанию `usd`),
  - инициализация `stripe.api_key`.

### cli.py

Подключается из `run.py` через `register_cli(app)` и добавляет команды:

- `flask --app run seed` — наполнить БД демоданными.
- `flask --app run create-admin` — создать/обновить администратора.

---

## 🌐 Blueprint shop (app/blueprints/shop/routes.py)

Отвечает за публичную часть магазина:

- `GET /` — главная страница каталога.
- `GET /product/<pid>` — карточка товара.
- `GET /cart` — просмотр корзины.
- `POST /cart/add/<pid>` — добавление товара в корзину (поддерживает AJAX).
- `POST /cart/api/update` — API для автообновления количества и суммы в корзине (используется на странице `cart.html`).
- `POST /cart/update` — «классическое» обновление корзины через форму.
- `POST /cart/clear` — очистка корзины.
- `GET/POST /checkout` — оформление заказа:
  - валидация имени, email и адреса,
  - создание заказа и позиций в БД,
  - попытка инициализации Stripe Checkout (если заданы ключи),
  - fallback‑сценарий без Stripe (`status="awaiting_payment"`).
- `GET /payment/success` — обработчик успешной оплаты (ставит статус `paid`).
- `GET /payment/cancel` — обработчик отмены оплаты (ставит статус `canceled`).

- какой маршрут обслуживается,
- какие параметры запроса используются,
- что возвращается.

---

## 👤 Blueprint auth (app/blueprints/auth/routes.py)

Отвечает за аутентификацию, профиль и кабинет:

- `/register` — регистрация:
  - проверка уникальности email,
  - проверка сложности пароля (`password_is_strong`),
  - создание пользователя и подсказка‑ссылка для подтверждения email (через flash).
- `/login` — вход.
- `/logout` — выход.
- `/confirm/send` — сгенерировать ссылку для подтверждения email (в dev‑режиме показывает ссылку во flash).
- `/confirm/<token>` — подтверждение email, токен действителен 24 часа.
- `/password/forgot` — форма запроса сброса пароля.
- `/password/reset/<token>` — ввод нового пароля, токен действует 30 минут.
- `/account` — личный кабинет: список заказов текущего пользователя (по `user_id` или по email).
- `/account/order/<order_id>` — детали конкретного заказа (проверяется доступ).
- `/profile` — страница профиля:
  - изменение имени и адреса по умолчанию,
  - загрузка аватара,
  - смена пароля (с проверкой текущего и политикой сложности).

---

## 🛠 Blueprint admin (app/blueprints/admin/routes.py)

Отвечает за админ‑панель. Доступ только при `is_admin=True`.

Маршруты:

- `/admin/` — сводная статистика (кол‑во товаров, категорий и заказов).
- `/admin/categories` — список категорий.
- `/admin/categories/new` — создание категории.
- `/admin/categories/<cid>/edit` — редактирование категории.
- `/admin/categories/<cid>/delete` — удаление категории.
- `/admin/products` — список товаров.
- `/admin/products/new` — создание товара (с загрузкой или указанием URL картинки).
- `/admin/products/<pid>/edit` — редактирование товара.
- `/admin/products/<pid>/delete` — удаление товара.
- `/admin/orders` — список заказов.
- `/admin/orders/<order_id>/status` — смена статуса заказа (`new`, `pending`, `awaiting_payment`, `paid`, `canceled`).


---

## 💳 Интеграция Stripe (тестовый режим)

Для использования Stripe нужно задать переменные окружения:

```bash
# пример для тестового режима
export STRIPE_SECRET_KEY="sk_test_..."
export STRIPE_PUBLISHABLE_KEY="pk_test_..."
export STRIPE_CURRENCY="usd"
```

Тогда при оформлении заказа `/checkout`:

1. Создаётся объект `Order` и позиции `OrderItem`.
2. Формируется список `line_items` для Stripe.
3. Через `stripe.checkout.Session.create()` создаётся платёжная сессия.
4. Пользователь перенаправляется на Stripe Checkout.
5. После успешной оплаты — редирект на `/payment/success?order_id=...`, где заказ помечается как `paid`.

Для теста можно использовать стандартные тестовые карты Stripe, например:

- успешная оплата: `4242 4242 4242 4242`,
- недостаточно средств: `4000 0000 0000 9995`,
- 3‑D Secure: `4000 0027 6000 3184`.

---

## 🧪 Тестирование вручную

Рекомендуемые сценарии «ручных» тестов:

1. **Регистрация и вход:**
   - зарегистрировать нового пользователя с паролем, удовлетворяющим требованиям;
   - попробовать неправильный пароль;
   - проверить, что отображаются подсказки по сложности пароля.

2. **Профиль и аватар:**
   - зайти в профиль,
   - изменить имя и адрес,
   - загрузить аватар и убедиться, что он отображается в шапке и в кабинете.

3. **Корзина:**
   - добавить несколько товаров,
   - изменить количество на странице корзины,
   - убедиться, что сумма пересчитывается автоматически без полной перезагрузки.

4. **Оформление заказа:**
   - оформить заказ без Stripe (без ключей) — увидеть статус `awaiting_payment`;
   - оформить заказ со Stripe (с тестовыми ключами) — увидеть статус `paid`.

5. **Админ‑панель:**
   - зайти под админом,
   - создать новую категорию и товар,
   - изменить и удалить товар,
   - сменить статус заказа.

---

## 🧠 Doc‑string'и

Во всех основных модулях (`app/__init__.py`, `cli.py`, `utils.py`, `blueprints/*/routes.py`) у функций добавлены подробные doc‑string:

- первая строка — краткое назначение функции,
- далее при необходимости — более детальное описание,
- секция `Args:` — аргументы функции,
- секция `Returns:` — возвращаемое значение.

Это облегчает:

- чтение и поддержку кода,
- автогенерацию документации (например, через Sphinx / mkdocstrings),
- использование IDE‑подсказок.

---

## 🔒 Замечания по безопасности и продакшену

В учебном проекте многие вещи упрощены. Для боевого использования рекомендуется:

- хранить секреты (Stripe, SECRET_KEY, SMTP) только в переменных окружения или менеджерах секретов;
- использовать HTTPS;
- вынести отправку email (подтверждение/восстановление пароля) на реальный SMTP или сервис вроде SendGrid/Mailgun;
- настроить миграции БД через Alembic / Flask‑Migrate;
- добавить более строгую обработку ошибок и логирование.

---

